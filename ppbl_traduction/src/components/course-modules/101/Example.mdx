import MDXLessonLayout from "@/src/components/lms/Lesson/MDXLessonLayout.tsx"

# My MDX page

This is a list in markdown:

- One
- Two
- Three

This is a `code here` example

```bash
sudo apt update
```

Checkout my React component:

```bash
sudo apt upgrade
sudo apt autoremove
```

Now a `python` example


```python
print("yeah")
```

and a block

```python
print("1st line")
print("2nd line")
```

and a jsx block

```tsx
console.log("hurray")
default const Check: string = "Hey yall"
````

export default ({children}) => <MDXLessonLayout>{children}</MDXLessonLayout>
